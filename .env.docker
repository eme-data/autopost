# ====================================
# CONFIGURATION DOCKER - AUTOPOST
# ====================================

# Configuration Serveur
NODE_ENV=production
EXTERNAL_PORT=127.0.0.1:8080

# Sécurité JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-characters
JWT_EXPIRES_IN=7d

# URLs
FRONTEND_URL=http://localhost

# HTTPS (Caddy)
DOMAIN_NAME=autopost.example.com
EMAIL_SSL=admin@example.com

# API - Intelligence Artificielle
GEMINI_API_KEY=your-gemini-api-key-here
GROQ_API_KEY=your-groq-api-key-here

# OAuth - LinkedIn
LINKEDIN_CLIENT_ID=your-linkedin-client-id
LINKEDIN_CLIENT_SECRET=your-linkedin-client-secret
LINKEDIN_REDIRECT_URI=http://localhost/api/oauth/linkedin/callback

# OAuth - Facebook
FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_APP_SECRET=your-facebook-app-secret
FACEBOOK_REDIRECT_URI=http://localhost/api/oauth/facebook/callback

# ====================================
# INSTRUCTIONS
# ====================================
# 1. Copiez ce fichier vers .env : cp .env.docker .env
# 2. Remplacez toutes les valeurs "your-*" par vos vraies clés
# 3. Changez le JWT_SECRET par une clé aléatoire forte
# 4. Pour production, utilisez votre domaine au lieu de localhost
